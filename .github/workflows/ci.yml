name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    # macOS mirrors the deployment target; the code itself is portable Go.
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: gofmt
        run: test -z "$(gofmt -l .)"
      - name: vet
        run: go vet ./...
      - name: test
        run: go test ./...
      - name: build
        run: go build ./...
